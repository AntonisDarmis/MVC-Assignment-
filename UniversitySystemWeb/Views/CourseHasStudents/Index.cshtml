@model IEnumerable<UniversitySystemWeb.Models.CourseHasStudent>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GradeCourseStudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseIdCourseNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentsRegistrationNumberNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GradeCourseStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseIdCourseNavigation.IdCourse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentsRegistrationNumberNavigation.RegistrationNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseIdCourse">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseIdCourse">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseIdCourse">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
